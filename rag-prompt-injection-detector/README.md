В папке rag должно лежать все, что нужно для rag, то есть класс с агентом, векторное хранилище и LLM

В папке detector лежит класс детектора, который собирается из сохраненной модели, принимает на вход embedding и context